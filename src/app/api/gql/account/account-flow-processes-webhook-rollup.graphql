query accountFlowsProcessesWebhookRollup($name: AccountName!) {
  accounts {
    byName(name: $name) {
      flows {
        processes {
          webhookRollup {
            ...FlowProcessGroupRollupData
          }
        }
      }
    }
  }
}
