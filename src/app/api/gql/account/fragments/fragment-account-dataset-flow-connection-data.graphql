fragment DatasetFlowProcessConnectionData on DatasetFlowProcessConnection {
  nodes {
    flowType
    dataset {
      ...DatasetBasics
    }
    summary {
      ...FlowProcessSummaryData
    }
  }
  totalCount
  pageInfo {
    ...DatasetPageInfo
  }
}
