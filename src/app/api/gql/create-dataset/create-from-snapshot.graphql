mutation createDatasetFromSnapshot($accountId: AccountID!, $snapshot: String!) {
  datasets {
    createFromSnapshot(
      accountId: $accountId
      snapshot: $snapshot
      snapshotFormat: YAML
    ) {
      message
      ... on CreateDatasetResultSuccess {
        dataset {
          ...DatasetBasics
        }
      }
    }
  }
}
