query datasetAsVersionedFile($datasetId: DatasetID!) {
  datasets {
    byId(datasetId: $datasetId) {
      ...DatasetBasics
      asVersionedFile {
        latest {
          ...VersionedFileEntryData
        }
      }
    }
  }
}
