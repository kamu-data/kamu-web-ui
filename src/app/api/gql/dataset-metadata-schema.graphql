query getDatasetMetadataSchema(
  $datasetId: DatasetID!
  $numRecords: Int
  $numPage: Int
) {
  datasets {
    byId(datasetId: $datasetId) {
      ...DatasetBasics
      metadata {
        ...DatasetMetadataDetails
      }
    }
  }
}
