fragment ReadStepCsvData on ReadStepCsv {
  separator
  encoding
  quote
  escape
  header
  inferSchema
  nullValue
  dateFormat
  timestampFormat
  schema(format: ODF_JSON) {
    ...DataSchemaInfo
  }
}
