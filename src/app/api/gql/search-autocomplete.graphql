query searchAutocomplete($query: String!, $perPage: Int) {
  search {
    query(query: $query, perPage: $perPage) {
      nodes {
        __typename
        ... on Dataset {
          id
          name
          kind
        }
      }
    }
  }
}